@model ProductViewModel

@{
    ViewData["Title"] = "Product Detail";
}

<div class="max-w-4xl mx-auto py-8">
    <div class="bg-white shadow-md rounded-lg overflow-hidden">
        <div class="px-4 py-2 bg-gray-100">
            <h2 class="text-lg font-semibold text-gray-800">Product Details</h2>
        </div>

        <div class="p-4 flex">
            <div class="mr-4">
                <img src="@Model.PictureUrl" alt="Product Image" class="h-60 w-auto" />
            </div>
            <div>
                <dl class="grid grid-cols-2 gap-x-4 gap-y-2">
                    <div>
                        <dt class="text-sm font-medium text-gray-600">Code:</dt>
                        <dd class="text-sm text-gray-800">@Model.Code</dd>
                    </div>

                    <div>
                        <dt class="text-sm font-medium text-gray-600">Name:</dt>
                        <dd class="text-sm text-gray-800">@Model.Name</dd>
                    </div>

                    <div>
                        <dt class="text-sm font-medium text-gray-600">Price:</dt>
                        <dd class="text-sm text-gray-800">$@Model.Price</dd>
                    </div>

                    <div class="col-span-2">
                        <dt class="text-sm font-medium text-gray-600">Description:</dt>
                        <dd class="text-sm text-gray-800">@Model.Description</dd>
                    </div>

                    <div>
                        <dt class="text-sm font-medium text-gray-600">Active:</dt>
                        <dd class="text-sm text-gray-800">
                            @if (Model.IsActive)
                            {
                                <span class="text-green-600">Yes</span>
                            }
                            else
                            {
                                <span class="text-red-600">No</span>
                            }
                        </dd>
                    </div>

                    <div>
                        <dt class="text-sm font-medium text-gray-600">New Release:</dt>
                        <dd class="text-sm text-gray-800">
                            @if (Model.IsNewRelease)
                            {
                                <span class="text-green-600">Yes</span>
                            }
                            else
                            {
                                <span class="text-red-600">No</span>
                            }
                        </dd>
                    </div>

                    @* If you want to display categories, you can add them here *@

                </dl>

                <div class="flex justify-end mt-4">
                    <a asp-action="Index" class="btn btn-secondary">Back</a>
                </div>
            </div>
        </div>
    </div>
</div>
